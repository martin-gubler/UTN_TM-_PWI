


/* 
function calcularSumatoria(a, b, c){        son lo mismo de 2 formas
    let sumatoria = a + b + c
    return sumatoria
}                                                           

function calcularSumatoria(a, b, c){        son lo mismo de 2 formas
    return a + b + c
}

calcularSumatoria(10, 20, 30)
 */
/* 
function devolverMayor(a, b){
    if(a > b){
    return a 
} else{
    return b
}
}

 */

/* 
function generarEmail(nombredeUsuario){
    return (nombredeUsuario + 'gmail.com')
} */


/* 
    CRAR UNA FUNCION QUE DADO 3 VALORES NOS DELVUELVA LA SUMATORIA
*/
/* 
const calcularSumatoria = (a, b, c) => {                son lo mismo
    return a + b + c
}   

const calcularSumatoria = (a, b, c) => a + b + c        son lo mismo



console.log(calcularSumatoria (1, 2, 3)) */
/* 
    validar string
*/

/* const Solicitar_y_validar_string = () =>{
    let stringsolicitado =prompt("ingrese un dato")
    while(!stringsolicitado || !isNaN(stringsolicitado)){
        alert("Error el dato debe ser un string")
    }
    return stringsolicitado
} */




/* CONVERTIR ESTAS FUNCIONES EN ARROW FUNCTIONS */

/* SIN RESOLVER
function mayor(a, b){
    if(a > b){
        return a
    } else{
        return b
    }
}

function generarMail(nombreDeUsuario){
    return String(nombreDeUsuario + '@gmail.com')
}

function sumar(a, b){
    a = Number(a)
    b = Number(b)
    return a + b + undefined
}
*/

/* 
RESUELTAS

const mayor = (a, b) =>{
    if(a > b){
        return a
    } else{
        return b
    }
}

const generarMail = (nombreDeUsuario) => nombreDeUsuario + '@gmail.com'

const sumar = (a, b) => a + b

 */