/* ## Calculadora de operaciones

Crear una funcion llamada calculadora() que de inicio a la sig logica
-Operaciones Disponibles:
    1) +
    2) -

Solicitar una operacion y verificar que la operacion sea valida (es valida si esta dentro de las operaciones disponibles)
Solicitar un numero 1 y validarlo
Solicitar un numero 2 y validarlo

Al finalizar las solicitudes mostrar el resultado por alerta de la sig manera:
'El resultado de {numero1} {operacion} {numero2} es {resultado} */

const calculadora = (numero1, operacion, numero2) =>{
    let numero1 = null
    let operacion = null
    let numero2 = null
}


let ingreseNumero1 = prompt ('ingrese un numero') 
    if (IngreseNumero1 !== isNaN(IngreseNumero1)){
    }
    return prompt('numero aceptado')


calculadora.numero1 = IngreseNumero1

let IngreseOperacion = prompt ('ingrese una operacion + o -')
    if (operacion === '+' || operacion === '-'){
    }
    return prompt('operacion aceptada')

    calculadora.operacion = IngreseOperacion

let IngreseNumero2 = prompt ('ingrese un numero')
    return (numero1 !== isNaN)

calculadora.numero2 = IngreseNumero2


